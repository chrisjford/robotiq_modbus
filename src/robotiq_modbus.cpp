#include <iostream>
#include "modbus.h"  // Ensure this points correctly to the modbus.h file

int main() {
    // Simple Hello World output
    std::cout << "Hello, World! Modbus setup is successful." << std::endl;

    // Test basic modbus.h inclusion (this won't do anything here, but ensures it compiles)
    modbus_t *ctx;
    
    return 0;
}
 